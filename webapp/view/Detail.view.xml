<mvc:View
    controllerName="sap.ui.demo.wt.controller.Detail"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc">
    <Page
        showNavButton="true"
        navButtonPress="onNavBack"
        title="{i18n>detailPageTitle}">
        <ObjectHeader
            intro="{invoiceItem>/ShipperName}"
            title="{invoiceItem>/ProductName}"/>
    </Page>
</mvc:View>